import 'package:flutter/material.dart';

class PersonFrame extends StatelessWidget {
  const PersonFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("PersonFrame"),
    );
  }
}