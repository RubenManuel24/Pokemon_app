import 'package:flutter/material.dart';

class FavoriteFrame extends StatelessWidget {
  const FavoriteFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("FavoriteFrame"),
    );
  }
}